{
  "name": "luno-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "Workspace root for the Luno backend SDK packages (see docs/backend-sdk.md)",
  "license": "Apache-2.0",
  "packageManager": "pnpm@11.1.2",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  }
}
